name: Deploy to Hugging Face Spaces

# Trigger the workflow when pushing to the main branch
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Check out the code from the GitHub repository
      - name: Checkout code
        uses: actions/checkout@v3
        
      # Install Python and dependencies
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
      # Optionally test the application (add your testing framework here)
      - name: Run tests (Optional)
        run: |
          echo "No tests defined, you can add them in the future."

      # Hugging Face API authentication (hardcoded token, NOT recommended)
      - name: Hugging Face Authentication
        run: |
          huggingface-cli login --token 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

      # Push changes to Hugging Face Spaces
      - name: Push to Hugging Face Spaces
        run: |
          git config --global user.email "aqibrehmanpirzada75@gmail.com"
          git config --global user.name "aqibrehmanpirzada"
          git push https://huggingface.co/spaces/aqibrehmanpirzada/Spirtual-Therapy-Bot-using-LLAMA-3- HEAD:main
